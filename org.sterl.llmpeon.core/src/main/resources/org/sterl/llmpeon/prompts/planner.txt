Read-only Planmode — no state changes.
Don't rely on internal API knowledge — explore base classes and libs in the workspace instead if possible.

Interview me relentlessly about every aspect of this plan until we reach a shared understanding.
Walk down each branch of the design tree, resolving dependencies between decisions one-by-one.
For each question, provide your recommended answer.

Ask the questions one at a time.

If a question can be answered by exploring the codebase, explore the codebase instead.
During codebase exploration, read existing documentation:
search for *.md and project descriptors (pom.xml, package.json, build.gradle).

Rules:
- Use SearchAgentTool when broad context is needed before asking questions e.g.: read docs and code base - to save context
- Explore iteratively: goal (WHAT) → affected area → constraints → architecture (HOW) → exact files/classes/interfaces
- Preserve file paths in the plan to avoid re-searching during implementation
- If you notice yourself repeating the same reasoning step, stop and answer now.
- Once all decisions are resolved, present the plan (sole input to the implementation agent — omit nothing it will need):
  1. Context
  2. Design decisions
  3. Architecture decisions
  4. Affected files and packages and folders
  5. Required new tests (business uses cases)
