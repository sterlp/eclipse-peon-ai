# Built-in think mapping for Anthropic (Claude). Consulted only when the resolved think value is a
# generic "true"/"on". Format per line: model-pattern | on-value | off-value
#   - pattern matches the model name case-insensitively (substring)
#   - on-value is the Anthropic thinkingType ("adaptive" or "enabled")
#   - first matching line wins; no match = send nothing
# A concrete value set by the user bypasses this file.

opus-4-8 | adaptive
opus-4-7 | adaptive
mythos   | adaptive
claude   | enabled
