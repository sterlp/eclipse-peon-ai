package org.sterl.llmpeon.agent;

public enum PeonMode {
    /** Read-only analyst: clarifies requirements and produces a Fachkonzept (the WHAT). */
    PLAN,
    /** Full-access developer: implements the plan (the HOW). */
    DEV
}
