# Resolved Points

Geklärte Punkte, die in kein Feature-Doc passen. Offene Punkte: [open-points.md](open-points.md).

| Punkt | Entscheidung | Begründung | Datum |
|---|---|---|---|
| `eclipseReadFile` kürzt lange Ausgaben still (Dev-Befund) | **Widerlegt** — kein Zeilen-Cap, R1d in [eclipse-read-tools.md](eclipse-read-tools.md) zurückgezogen | Pfadanalyse `FileLines` → `DefaultToolExecutor` → `SmartToolExecutor` → `ToolService.execute` → `ThreadSafeMemory.addResult` → `ToolLoopRequest.call` → `StreamingBridge`: nirgends wird gekappt. `ChatMessageUtil`-Limits (6000/90000/900000) dienen Logging/Dedup/Token-Schätzung; `SmartToolExecutor:60-62` kürzt nur Anzeigetext. Beobachtet wurde R1a/R1b. | 2026-09-03 |
| „Loading 📋 Static env info" erscheint häufig nach Tool-Calls — System-Prompt-Bug? | **Kein Bug.** Kein Rebuild pro Tool-Iteration, Prompt-Cache bricht nicht | `ToolService.executeLoop` ruft `buildSystemPrompt()` nie; Static-Context enthält nur `LocalDate.now()`, keinen Timestamp/Projekt/Zähler. Die Meldungen stammen von **verschiedenen Agenten** (je eigener Cache) und echten Invalidierungen (`clear`/`compact`/`setStaticContext`/`updateConfig`). Sichtbar erst seit inc-27 (Agentenname in der Meldung). Rest-Befunde (doppelter Aufruf `AbstractAgent:179`, fehlende `count == 1`-Assertion) → Zyklus 2a. | 2026-09-03 |
| Gewähltes Projekt im Static Context? Bekommen Da Mek/Da Thinka es? | **Wie gewollt** — Projekt ist Turn-Context, nicht System-Prompt; Sklaven bekommen es bewusst nicht | `AgentContextComponent:136-140` → `UserContext:20-44` (aktiver Agent). `initStaticContext` fügt nur `StaticContextItem` hinzu; `BuildPoAgentComponent:99-112` gibt den Sklaven Plan + agentenspezifische AGENTS.md + Workspace-Memory, kein Selected-Project-Block. Entspricht `issues/fact-issues.md` Punkt 0b. | 2026-09-03 |
| Custom-Dropdown-Umbau (Chat-Agent-Picker + Model-Dropdown) | **Descoped** — Klassen 2026-09-06 **ersatzlos gelöscht** (`a1d8d35`, Dead Code, 0 Referenzen, −646 Zeilen; `DropdownItem`/`DropdownTheme`/`DropdownButton`/`DropdownPopup`) | Der halbfertige Umbau brach den Build (`DropdownButton:83`, nicht existentes `Control.setTabList(boolean)`) unmittelbar vor dem Release. Kosmetik vs. Release-Stabilität — User-Priorität: Release. Nach dem Descoping lagen die Klassen unbenutzt kompilierbar herum; User-Entscheidung 2026-09-06: löschen statt liegen lassen. Wiederaufnahme = eigene Story (Code aus Git-Historie). | 2026-09-03 (descoped), 2026-09-06 (gelöscht) |
| `ModelListCacheTest.concurrentFailure_…` flaky (Review-Befund 2b-3) | **Gefixt** — zweite Barriere statt Timing-Fenster, Klasse 5× separat grün | Der Test startete `t2` und rief unmittelbar danach `release.countDown()`; lief `t1` vorher durch, holte `t2` selbst und bekam `models("m1")` statt `null`. Produktcode war korrekt (Single-Flight ist durch `concurrentGetOrFetch_sameIdentity_singleFlight` abgedeckt) — ein flakiger Test hätte künftige Abnahmen fälschlich rot gemacht. | 2026-09-03 |
| `iso-test.txt` im Testbundle | **War nie ISO-8859-1**, sondern UTF-8 — Fixture-Datei per Code neu erzeugt | `xxd` zeigte für Quelle *und* Kopie `C3 A4 C3 BC C3 9F 20 C3 96`. Der Test `readIso` prüfte also nie echtes ISO-Verhalten. Neue Fixture-Datei `test_project/data/iso-test.txt` mit echten ISO-Bytes + `.settings`-Encoding-Eintrag; `readIso` entsprechend verschärft. | 2026-09-03 |
| Backslash-Pfade in den File-Tools (`a\b.txt` vs `a/b.txt`) | **Keine Normalisierung in den Tools** — Windows versteht beide Separatoren nativ (Path + Eclipse `IPath`); POSIX: LLM-Vertrag ist `/`, Worst Case = ehrliches „file not found" (self-healing). Nur der Validator normalisiert unbedingt ([write-path-validator.md](write-path-validator.md)) | Normalisierung in den Tools würde wörtlich `a\b.txt` genannte Files auf POSIX unadressierbar machen — ein realer Verlust für eine hypothetische LLM-Verfehlung. Tool-Mismatch = lauter, ehrlicher Fehler (LLM retryt); Validator-Mismatch = stiller Bypass (Security) — verschiedene Failure-Modi rechtfertigen verschiedene Behandlung. | 2026-09-04 |
| `eclipseWriteFile` schreibt immer UTF-8 (Charset-Asymmetrie, Triage 2026-09-03) | **Widerlegt** — kein Bug. `IoUtils` schreibt seit v0.1.0 (#3) mit `IFile#getCharset()` (UTF-8-Fallback für neue Dateien); Regression-Test mit ISO-8859-1-Fixture läuft grün auf unmodifiziertem Code (`27c09ad`) | Triage-Befund war ein Fixture-Artefakt: Charset wurde per prefs-Datei auf der Disk gesetzt, **ohne Workspace-Refresh** — der Workspace sah weiterhin das UTF-8-Project-Default. Der Test bleibt als Regression-Guard. | 2026-09-06 |
| PDE-Runner meldet Skips nicht separat | **Erledigt** — `EclipseRunTestTool` meldet `Skipped: N` (`11f34f6`) | JDT `testCaseFinished` feuert auch für Ignored mit `Result.IGNORED`, gezählt im bestehenden Listener; Maven-Semantik („Tests" = total inkl. skipped). Canary-Test (temporäres `@Ignore`) verifizierte die Zählung. Zeile sichtbar ab erstem Lauf nach Eclipse-Restart (Report-Formatierung im laufenden Bundle). R5-Einschränkung in [test-setup.md](test-setup.md) aufgelöst. | 2026-09-06 |
