# Docs — design & dev spec (the HOW / system reference)

The `docs/` tree is our shared memory: one story per feature (business rules + BDD). Your technical
notes live in [adr/](adr/index.md). Not published — user-facing docs are in `homepage/`.

**[Glossar — Ubiquitous Language](glossary.md)** — ein Begriff, eine Bedeutung, in Docs wie im
Code. Vor dem Einführen eines neuen fachlichen Begriffs dort nachsehen und eintragen.

**[Configuration Overview](configuration.md)** — Landkarte der Konfiguration: wo steht was
(Preferences · `~/.peon` · Code-Defaults). Details in den verlinkten Story-Docs.

## Stories

* [Architecture — Component Rules](architecture.md) - Service/Component/View/Widget layering, extraction rules, base packages. **✅ 2026-08-23:** PeonAiService = Fassade über SharedTools-/AgentContext-/BuildPoAgentComponent.
* [Context Architecture](context-architecture.md) - static (System-Prompt) vs dynamic (Chat History) — wer bekommt was, lazy-loading, UI reporting. **2026-08-21:** Static trägt Env+Memory — [ADR-0031](adr/0031-static-context-env-plus-memory.md). **2026-08-23 (✅ done):** Workspace-Memory dynamisch pro Turn (auch Sklaven via Delegate-Orders); statischer Snapshot entfernt → Static = nur Env ([ADR-0032](adr/0032-workspace-memory-dynamic-turn-context.md), Code-Umsetzung durch User). **2026-09-23:** Static trägt User-Language-Fallback (Vorrang: AGENTS.md > Chat-Sprache > System-Locale).
* [Disk File Write Tool](disk-file-write-tool.md) - real filesystem write/edit tools, configurable workingDir, disabled by default. **2026-09-04 (✅):** `diskEditFile` = Replace-All + Count-Disclosure („replaced/deleted N occurrence(s)"), `newString=null` = Delete — disk+eclipse konsistent. **2026-09-06 (✅ E5):** Success-Messages nennen den **absoluten Pfad** (workingDir aufgelöst) — im Sync mit den Eclipse-Tools.
* [Eclipse Workspace Write Tool](eclipse-workspace-write-file-tool.md) - Eclipse VFS write/edit tools, project-scoped sandbox, always available.
* [Write-Path Validator](write-path-validator.md) - per-agent `WriteValidator` (agent-provided like the tool filter) that vets the normalized write path at the tool choke-point; scopes Jon to `*/docs/*` + `*.md`. **2026-09-04 (✅):** Normalisierung vor Glob-Match — Traversal-Bypass (`a/docs/../../secret.txt`) geschlossen, BDD R1 um Traversal-Fall. **2026-09-04 (follow-up #9):** Normalisierung = `\`→`/` + `.`/`..`-Auflösung, unbedingt auf allen Plattformen — gemischte Separator-Traversals (`a/docs/..\..\secret.txt`) geschlossen.
* [Advanced Configuration](advanced-configuration.md) - the two-page preference split and per-agent model resolution via `ChatRequest.modelName()`. **2026-08-21:** SOLL agent-spezifischer Config-Umbau (alles pro Agent, inkl. JSON extra body) — [caching.md](caching.md). **2026-08-30 (✅ 2b):** per-agent Model-Config (URL/Key/Modell/Think/JSON-Body) + Connection-Cache nach Identität — [ADR-0034](adr/0034-connection-cache-by-identity.md); Modell-Dropdown + Think aus Chat-UI raus → Config-Seite (+ Refresh-Button); Core/UI-Trennung, kompletter Config-Rebuild bei Update. **2026-09-03 (✅ 3a):** eigener PO-Model-Slot (R-PO1…R-PO4, [ADR-0036](adr/0036-po-own-model-slot.md), supersedes 0023) — `CORE_IDS` als einzige Slot-Aufzählung, Fallback = Base statt Plan, alle 6 harten PLAN-Bezüge in `AiPoAgent` entfernt. **2026-09-03 (✅ 3b):** Temperature-Feld pro Agent (R-T1…R-T5) — leer = unset, kein Slider, ein zentraler Parse-Punkt `AgentTemperature` für Core-Slots **und** Custom Agents, extra body gewinnt via Streichen des typisierten Feldes ([ADR-0039](adr/0039-temperature-body-precedence.md)); Clean Break der Base-Level-Keys inkl. aller toten `getTemperature()`-Overrides und des `DoubleSliderFieldEditor`. **Search/Compact verlieren dabei ihre impliziten 0.3/0.2** — ohne eigenen Wert wird nichts gesendet. **2026-09-01 (✅ 2c):** Cache-Hardcode-Clean-Break (Caching nur via extra body) + GPT/Claude-UI-Beispiele (Paste-Buttons) + Cache-Usage im Token-Header (`↑ ↓ ⇄`) + Custom Agents via AGENT.md-Frontmatter (`url`/`api_key`/`extra_body`) + Homepage-Rewrite.
* [MCP Servers](mcp.md) - MCP-Server-Anbindung (STDIO/Streamable-HTTP), Protocol-Version-Semantik, Live-Apply. **✅ done (2026-09-10, mcp-fixes):** R-MCP1 live-apply bei MCP-Config-Änderung (Compare in `McpConnectionService` statt LlmConfig-Gate); R-MCP2 leer = Auto-Detect, kein stiller Default (Era-Lock-`-32022`-Fall: [ADR-0045](adr/0045-mcp-protocol-version-auto-detect.md)); R-MCP3 editierbare Combo. R-MCP3-Verifikation: manuell (User-Smoke-Test).
* [Custom Agents](custom-agents-design.md) - user-defined `AGENT.md` agents with tool allowlists, read-only mode and per-agent model. **2026-09-04 (✅):** null-Allowlist (kein `tools:`) = all Tools, vor read-only-Regel; Inline-CSV-Flattening; Homepage korrigiert (doku­mentierte das Bug-Verhalten). **2026-08-28:** komplette Model-Config auch via AGENT.md-Frontmatter (yml) — **✅ gebaut (2c, 2026-09-01):** Keys `url`/`api_key`/`extra_body` → gleicher `AgentModelConfig`-Record wie Core-Agents; SOLL in [advanced-configuration.md](advanced-configuration.md), Homepage-Doku ✅.
* [Agent Ordering](agent-ordering.md) - **✅ done (#128, 2026-09-06):** `agent-order.txt` (Regex-Zeilen, Full-Match) steuert die Dropdown-Reihenfolge aller Agenten; Auto-Create mit Default `^Peon-PO$`.
* [Read Tools (Eclipse + Disk)](eclipse-read-tools.md) - **✅ done (2026-09-03) — R1–R7 komplett.** Ein Verhalten für beide Tool-Familien (geteilte `FileLines`/`SearchQuery`/`AiReponseBuilder`): Read clampt Bereiche statt Ganzdatei ✅; Grep Regex-first mit Literal-Fallback, Modus **immer** im Ergebnis ✅ ([ADR-0035](adr/0035-grep-regex-first-literal-fallback.md) + Nachtrag: `C++` ist gültiges Regex). **R4 ✅ done (2b-1):** `eclipseSearchFiles` — gewähltes Projekt zuerst, Limit global, Dedup nach Disk-Pfad, Derived-Filter ausnahmslos, Refresh nur bei leerem Ergebnis ([ADR-0038](adr/0038-refresh-on-empty-search.md)), Leermeldung nennt Scope + übersetztes Pattern, `JdtUtil`-Projekt-Scope repariert. **R7/R6/R6a ✅ done (2b-2, 2026-09-03):** `eclipseGrepFiles` erbt das R4-Muster (gewähltes Projekt zuerst, Refresh-on-empty per ADR-0038, Leermeldung nennt Scope+Pattern); geteilte `TextFileTypes` im core (Vereinigung beider Whitelists + `FILE_NAMES` für `Dockerfile`/`Makefile`), Typ-Filter wird bei leerem Ergebnis benannt. **R3 + R7d ✅ done (2b-3, 2026-09-03):** `eclipseReadConsoleLog` bekommt `grep` (Regex-first/Literal-Fallback), `lines` limitiert die **gefilterten** Zeilen, Header nennt gezeigt/gesamt + Suchmodus — Logik als **Log-Auszug** `LogExcerpt` im core; Refresh-Ziel bei leerem Ergebnis trifft nur noch das gewählte bzw. explizit genannte Projekt ([ADR-0038](adr/0038-refresh-on-empty-search.md) Nachtrag). **Story komplett — R1–R7 alle ✅.** **ℹ️ Info (2026-09-16):** Projektname ≠ Disk-Ordnername lässt `disk*`-Tools an workspace-qualifizierten Pfaden scheitern — bewusst **kein** Code-Fix (Mehrdeutigkeit, WriteValidator-Grenze, Familien-Trennung), Empfehlung „Projektname = Ordnername" auf der Homepage.
* [Test-Setup (OSGi)](test-setup.md) - **✅ done (2026-09-03).** Dediziertes `test_project`-Fixture statt Selbst-Import, Pfad via `peon.test.project`, fehlendes Fixture = Fehler statt Skip, `askclear=false` für unattended Läufe ([ADR-0037](adr/0037-dedicated-test-fixture-project.md)).
* [Maven Pipeline (CI)](pipeline.md) - CI headless-SWT-Verträglichkeit: `EclipseConsoleLogToolTest.after` crasht in CI (`SWTError: No more handles`) — neuer `ConsoleZoomHandler` in `org.eclipse.ui.console` 3.17.100 feuert `Display.getDefault()` auf `removeConsoles` (Target-Sprung 2026-03→2026-09). **✅ done (2026-09-10):** Fix = `xvfb-run -a mvn -B clean verify` in maven.yml; sequenziell validiert (B Guard grün → A ohne B grün); B-Guard bewusst NICHT zurückgebracht (maskiert Environment-Drift, AGENTS „guard that never fires is worse than none").
* [Interaction Design](interaction-design.md) - the chat view layout: history, input block, action bar and status line.
* [Chat Job Lifecycle](chat-job-lifecycle.md) - Job-Submit→Lock→`monitorRef`→finally→Unlock + Stop-Semantik. **✅ gebaut (2026-09-10):** R-ST1 In-Flight-Counter (Clobber-Race-Fix: stale finally entlockt nicht mehr den lebenden Run; Phantom-Job-Loch ebenfalls geschlossen; Counter-Invarianten 1:1/never-below-0 fail-open + Mutations-Nachweis), R-ST2 Graceful Stop (Noop wenn nichts läuft, kein Code-Change), R-ST3 Turn-INFO-Logging mit Skip-Beweis. Verifikation manuell (User-Smoke, Plan §7).
* [Plan & Dev Agent](plan-dev-agent-design.md) - the two-phase plan→dev handoff model and its planned pipeline features. **2026-09-05 (✅ R-PI1):** `planImplemented`-Archiv: Counter-Suffix bei Namens-Kollision (core `ArchiveName.firstFreeName`), nie "already exists".
* [Model Loading](model-loading.md) - model list lifecycle: lazy fetch, persistence across agent switches, fallback on failure. **2026-08-30 (✅ 2b):** Dropdown in der Config-Seite; Liste einmalig pro Verbindungs-Identität gecacht, Cache on success, Refresh-Button, kein Auto-Switch (ADR-0034). **2026-09-02 (✅ inc-25):** Basic-Page + per-agent gemeinsam `ModelComboWidget`; Single-Flight pro Identität statt Cancel + Secret-Masking in `toString()` (inc-24, [ADR-0040](adr/0040-model-list-single-flight-secret-masking.md)). **2026-09-10 (✅ R-ML1):** Fetch-Identity zur Fetch-Zeit — Advanced-Page nutzt live-Supplier statt Stale-Snapshot; Base-URL-Korrektur greift ohne Page-Reopen. **2026-09-12 (✅ R-ML3):** natives SWT-Combo auf beiden Pages + Refresh-Button unter dem Combo (`ModelComboWidget` = Controller). **2026-09-12 (✅ R-ML4):** Eingabe-Dedup gegen die Server-Liste — case-insensitive, canonical = Server-ID, Edge ohne Server-Liste verbatim, **No-Append** (getippte Eingabe wird nie Listeneintrag); final `985b244` (Inc-5), User-Re-Smoke ✅ 2026-09-13.
* [Per-Agent Think Support](per-agent-think.md) - per-agent thinking support and request-value resolution via provider mapping files and AGENT.md frontmatter.
* [Queued User Messages](queued-user-messages.md) - input queue with batching, FIFO consumption, drain-to-memory on abort. **❌ specified (2026-09-23):** Queued-At Disclosure (Regel 9) — queuedAt je Eintrag, Uhrzeit in onTool-Zeile UND `[Queued Message]`-Marker.
* [Session Token Usage](token-usage.md) - cumulative ↑/↓ token spend in the header, fed from the StreamingBridge choke point.
* [Scaffold Agent](scaffold-agent.md) - built-in agent for creating/editing Peon config artifacts (agents, skills, commands) with config-scoped disk tools.
* [Project Skills](project-skills.md) - **✅ done (2026-09-08, Zyklus story/133):** projekt-lokale Skills in `<project>/.agents/skills`: Override über Config-Skills (nur beim Lesen, disunkte Slot-Component-Maps), Slot-Wechsel am setProject-Choke-Point (pinned schlägt Selektion), Scaffold schreibt in .agents/skills aller offenen Projekte (dynamischer WriteValidator) + fragt die Platzierung im Chat. **Follow-up ✅ done (2026-09-09, Story A):** Component-Refactor — SkillSlot→SkillComponent (immer ein Pfad, kein @Nullable; Service tauscht Instanz, kein Cache), Scaffold-Write→refreshAll() deterministisch, Jon bekommt geteilten SkillTool (R14–R16).
* [Skill-Evolution-Loop](skill-evolution-loop.md) - **✅ done (2026-09-09, story/133):** Skills müssen Token-Wert beweisen — CRUD-Zyklus (Create aus gelösten schwierigen Aufgaben, Update immer verbessern/kurz halten, Read-Nutzung reporten via Footer ✅ `b4e918d`, Delete bei Schaden), Jon nimmt Skill-Änderungen ins Iteration-End-Summary, experimentell → AGENTS-PO.md/AGENTS-DEV.md statt Built-in-Prompts, Loop darf sich selbst reifen.
* [`peon` Config Directory](peon-config-directory.md) - **✅ done (R1+R2+R3, 2026-09-06):** `~/.peon` = shared config only; Agent-History → workspace Metadata-State (getStateLocation) + One-Shot-Migration von `~/.peon/state` ([ADR-0041](adr/0041-peon-shared-config-only.md)). Side Quest: Jon delegiert an Scaffold.
* [Built-in Agent Prompt Override](builtin-agent-prompt-override.md) - **🚧 Idee (2026-09-06, nicht geplant):** Built-in-Prompts als Config in `.peon` — Body supersedes den Default; Improvement-Cycle via geteilte Prompts.
* [Standing Orders](standing-orders-design.md) - **historisch (superseded 2026-08-21).** `StandingOrdersBuilder` gelöscht; aktueller Flow: [Context Architecture](context-architecture.md) + [ADR-0031](adr/0031-static-context-env-plus-memory.md).
* [AGENTS.md Support](agents-md-support.md) - base AGENTS.md loading: purpose, file name resolution, toggle. **✅ 2026-08-16:** History statt System-Prompt (Pfad-Dedup) — [ADR-0029](adr/0029-file-context-in-history.md).
* [Agent-Specific AGENTS-<agent>.md](agent-specific-agentsmd.md) - AGENTS-<agent>.md: agent name resolution, case-insensitive fallback, deduplication.
* [SWT Integrated Input Buttons](swt-integrated-input-buttons.md) - flat icon buttons beside a `StyledText` that read as one white field on macOS + Windows.
* [Ask User Tool](user-question-tool-design.md) - the LLM pausing mid-task to ask a clarifying question inline in the chat. **R-UI1 ✅ (2026-09-06, inc-3):** Frage bleibt beim Widget-Erscheinen vollständig sichtbar — finaler Scroll-to-bottom nach Widget-Completion (ScrollToBottomCommand → chat.html); manuelle Verifikation (kein SWT-Test-Harness).
* [Persistent Agent History](persistent-agent-history.md) - JSONL chat history persistence for Dev, Plan and custom agents.
* [Streaming Response Display](streaming-display.md) - status-bar overlay with bounded live preview, single DOM insert on completion, no incremental chat rendering. **2026-09-05 (✅ R22):** kein toc/s am 1. Token eines Calls (Rate 0, ab 2. Token wie R19, pro Call via `tokenPhaseStart`-Wechsel). **2026-09-05 (✅ R18–R21):** Timer-Klasse (`shared.Timer`, volatile, Clock-injizierbar) + 4 Timer in StreamingBridge (Total/PP/Token/Think); toc/s = Estimator-Tokens / Token-Timer-Dauer statt Turn-Start; TOOL zählt `partialArguments()`-Delta; "Started hh:mm · working since X" in Statusleiste; kein n/a-Threshold.
* [Sub-agent tool timing](sub-agent-timing.md) - the sub-agent tools (talkPlan/planWithPlanAgent/askDev/buildWithDev, searchAgent, compactSession) append the nested agent's wall-clock to their done line, e.g. `done. (3s)`.
* [Search Agent Tool (Da Sniffa)](search-agent-tool.md) - stateless one-shot research sub-agent: read-only tools, dedicated search model, thinking disabled, no shell/ask/memory.
* [Agenten-Namen im Chat-Header](agenten-namen-im-chat.md) - **WIP-Design.** AI-Header zeigt den sprechenden Agenten (Peon-PO/-Plan/-Dev, Custom-Agents, Da Sniffa/Da Scribe) statt „Peon"; Name reist auf dem `ToolLoopRequest` mit, kein Monitor-Umbau.
* [Agenten-Status im Header](agenten-status-im-header.md) - **Roster ✅ gebaut** (MVP-Neubau: Pull/MVC, `getTeam()` mit Da Boss/Da Thinka/Da Mek/Da Dok, Blatt-Regel, [ADR-0025](adr/0025-po-status-widget-named-agents.md)). **✅ done (2026-09-14, story/po-compact-2026-09-13):** Per-Sklave Compact-Button im Header (Da Thinka/Da Mek/Da Dok, Icon-only) — komprimiert genau diesen Agenten; User-Smoke steht aus.
* [Agenten-Status Header — MVP-Neubau (Plan)](agenten-status-im-header-mvp-plan.md) - **WIP-Plan.** Pull/MVC statt Observer ([ADR-0025](adr/0025-po-status-widget-named-agents.md)): `AiPoAgent.getTeam()` → `List<NamedAgent>` (Ork-Sklaven Da Thinka/Da Mek) → `AiAgentStatusWidget`; ein `instanceof`-Choke-Point in `PeonAiService`; `getRoster`/`onSubAgent`/`workingSubAgents` fallen weg. Inkremente einzeln baubar.
* [Agent-API-Retry (Mini)](agents-retry.md) - **✅ done.** Eigene Klasse `streaming.ApiRetry` am Choke-Point `ToolLoopRequest.call` für **alle Agenten & Kontexte**: verdiente Geduld (`credit` +1/Erfolg, max 10) + `retryCount` (Reset/Erfolg) → linearer Backoff `min(retryCount·10s, maxWait)`, Sleep in `min(1s, restWait)`-Häppchen, Cancel nie retryt (Klassifikation), Root im Wrapper geloggt, englische `onProblem`-Meldung.
* [Peon-PO (Jon)](po-agent-jon.md) - docs-owning business-owner agent that designs features and orchestrates its own Peon-Plan/Peon-Dev via talkPlan/planWithPlanAgent/askDev/buildWithDev (+ searchAgent) with planComplete/planImplemented completion signals. **2026-08-29:** Prompt-BDDs durch Prompt-Referenzen ersetzt (neue Prompts-Sektion mit Wiring-Tabelle) — Content lebt im Repo, Docs halten das Wiring; R15 ✅ (prompt-basiert). **2026-09-05 (✅ R16):** Compact-Guard — < 2 Messages → Skip statt LLM-Call (kein Clear; SOLL-Korrektur 2026-09-13, zuvor fälschlich „Reset"). **2026-09-05 (✅ R17):** Jon bekommt `askUser` (matched User-Antworten, queue-safe, cancel-safe; Slaven-/Search-Filter bleibt). **2026-09-13:** **R18 ✅ done (2026-09-14, story/po-compact-2026-09-13; Mutation-Proof)** Compact ohne Cascade — nur der Agent selbst, impliziter Slave-Cascade entfernt; **R19 ✅** Clear-Cascade bleibt (Jon-Clear räumt alles weg) — Homepage-Doku ✅ `usage/agents.md`.
* [Review Agent (Da Dok)](review-agent.md) - **✅ done (2026-09-09, story/133):** dritter PO-RAM-Sklave `Peon-Review` (AiPlanAgent-Familie, read-only, PLAN-Slot): Reviews via `reviewPlanAgent` statt talkPlan — frischer Context, Verdict ACCEPTED/CONCERNS/REJECTED, 3-Seiten-Prüfung, repariert nichts. **2026-09-09:** Team-Reihenfolge Da Boss · Da Thinka · Da Mek · Da Dok (User-Entscheid).
* [Prompt Inventory & Wiring](prompts.md) - **✅ done (2026-08-29).** Die 12 Built-in-Prompt-Dateien: welche Datei, wer lädt sie, wann, warum der Split — nur Wiring, kein Content (Repo = SOT); keine Prompt-BDDs in den Feature-Docs. Loader `PromptLoader`/`PeonPaths`.
* [Sklaven-Kontext](sklaven-kontext-plan.md) - Jons RAM-Sklaven (Da Thinka/Da Mek) bekommen denselben relevanten Kontext wie der aktive Agent: gewähltes Projekt + `AGENTS.md`-Basis (`getBaseAgentsMd`) + Static-Context Datum/OS/File-Regeln (`setStaticContext` auch auf die Sklaven) — **alle ✅ gebaut, grün**; Editor-Selektion bewusst nicht, `AGENTS-DEV/PLAN.md` per Sklave = Backlog (Inc 3). **2026-09-16 (✅, Paul `29a341b`):** Namen im System-Prompt + Agent Mode — R-N1 Name per Fan-out (`ContextItem.newList`, build()-Injektion = Standalone-Fallback), R-N2 `AGENT_MODE` (offene Fragen explizit in der Antwort, nie raten, kein `askUser` für Slaven), R-N3 Prompt-Datei trägt den Namen nicht mehr.
* [Async Agent Tools](async-agent-tools-proposal.md) - **Proposal.** Sync Agent-Tools blockieren Queue. 3 Optionen, Option C (Queue-basiert) empfohlen.
* [Prompt Caching](caching.md) - **✅ done (2c + R8, 2026-09-01).** Caching als **per-agent JSON extra body** (Advanced Settings, auch Custom Agents via AGENT.md-Frontmatter) mit GPT-/Claude-UI-Beispielen (Paste-Buttons, Gate `supportsExtraBody()`); gilt für jeden Agenten mit Modell-Slot; llama.cpp: kein Snippet = kein Slot belegt; Abgleich über Usage-Cache-Tokens im Token-Header (`↑ ↓ ⇄`). **Ist-Hardcode (Anthropic/OpenAI) entfernt (Clean Break):** Caching lebt nur im extra body — kein stiller Default, keine Migration; Claude-Direkt-API verliert natives Caching (Homepage-dokumentiert). R1a: Body per-request wo möglich (verifiziert: OpenAI-Familie; Anthropic Build-time), sonst UI-Gate. **R8 ✅: GPT-5*-Agenten bekommen Default `prompt_cache_key = peon-ai-<agent>`** (Provider-Entry, User-Wins via 2a-Merge, kein Breakpoint-Scope). **2026-09-12 (✅ R-A1/R-A2):** Config-Page UI — kein Extra-Fußraum unter den Examples (`GridData.exclude`-Pattern) + natives Model-Combo auf beiden Pages (`ModelComboWidget` = Controller, Verhalten unverändert). Think-Dropdown-Follow-up R-A4 ✅ done (natives editierbares Combo, ui-config Inc-3), Copilot-Studie R-A3 ❌ separat.
* [UI-Icons — Rolle & Typografie](icons.md) - **❌ specified (2026-08-30/31, Night-Cycle C).** SOLL-Definition: Funktions-Rollen (🗜 Compact, 🧩 SKILL, 🪄 Command) + Agenten-Personen (offen, Q1) + Kandidaten-Pool + Typografie-Regeln (Font-Size, Plattform-Fallback, Position); Umsetzung = eigener Build-Zyklus (UI-Widgets), NICHT in 2c.
* [Shell Tool](shell-tool.md) - read-only diagnostic shell with tool-side output control (tail limit, regex filter) instead of shell pipes. **2026-09-04 (✅ done):** R1 tail-Semantik (Default 60, >0 = letzte N, <=0 = all, Hard-Cap 3000 disclosed) + R2 `filter` (Regex-first/Literal-Fallback, filtern → tail auf Gefiltertes, Disclosure `showing N of M`).
* [File Copy Tool](file-copy-tool.md) - **✅ done (R1–R6, 2026-09-06, Zyklus file-copy-e2e-fixes):** Byte-Copy ohne Overwrite; R5 Qualified Paths Only (Copy+Rename, beide Familien — Disk absolut, Eclipse `/project/path`, `QualifiedPathValidator`); R6 Erfolgsmeldung LLM-sichtbar (`Copied/Renamed <s> -> <t>`, nicht langchain4j-`"Success"`). E2E-Befunde behoben.
* [Eclipse Java Move Type](eclipse-java-move-type-tool.md) - **🚧 in design (2026-09-06, Backlog):** JDT-Refactoring — Java-Type rename/move inkl. aller Referenzen; Signatur/Scope/Runner-Details offen.
* [Provider (AiProvider)](provider.md) - **✅ done (2026-08-28).** Eigene Komponente: `AiProvider`-Enum → Interface + je Provider eine Klasse (core-Package, kein neues Bundle); Extra-Body-Fähigkeit (`supportsExtraBody()` — **nur dann UI-Eingabe**, caching.md R2) + Think-Support-Fähigkeit (Boolean/Values/FreeString/None/Unknown) je Klasse; verhaltenstreu.

* [Docs-Linter](docs-linter.md) - **✅ done (2026-09-15):** deterministischer Abgleich Use-Case-IDs (`#### UC-<FEATURE>-<n>` als Überschrift) gegen ID-Kommentare am Test (`// UC-…`); drei Methoden (`lintDocsAndTests`/`lintDocs`/`nextIds`), Befundarten inkl. `UNBELEGT_ERLEDIGT`/`VERWAIST`/`DOPPELT_DEFINIERT`, Report mit Gesamtzahlen + namentlichen Skips, nie Build-Schranke. Ziel: ein falsches `✅` maschinell unmöglich machen. Teilnahme per Opt-in über Frontmatter `idPrefix`; nur Dateisystem-Zugriff mit einem `root` ([ADR-0047](adr/0047-docs-linter-disk-only-single-root.md)). **Korrektur im Bau (Q8):** Methodenkopf-Zwang gestrichen — die ID-Zeile allein ist der Beleg, sonst verfiel er bei `it(...)`/`export function` still (False Negative); `testGlobs` plural, Default-Testtypen aus `TextFileTypes` ohne Text-/Datenformate, `docRoots` vom Test-Scan ausgeschlossen. **Q9:** Report nennt gefundene Dateizahlen, damit „falscher Pfad" nicht wie „sauberer Bestand" aussieht. **R-DL-13 ✅ (2026-09-15):** Fenced Code Blocks sind zitierter Text, keine Definitionen — Dogfooding-Fund aus dem ersten Lauf gegen das eigene Repo. **Nachzyklus R-DL-7/8/14/15 ✅ (2026-09-15, [ADR-0048](adr/0048-docs-linter-read-only-and-tool-split.md)):** `reportPath` ersatzlos gestrichen → echt read-only (der Rückgabewert trug ohnehin alles ungekappt; Traversal-Risiko, Write-Validator-Kopplung und `isEditTool()`-Inkonsistenz entfallen durch Streichen statt Absichern); Fassaden-Split `DocsIdTool` (`nextIds`, **nur Jon** — vergeben darf einer, prüfen dürfen alle) / `DocsLinterTool` (beide Prüfmethoden, alle Agenten); 🐞 raus aus dem `diskToolsEnabled`-Gate, hinter dem das Tool per Default für **niemanden** existierte, obwohl drei Prompts es verlangten; erstmals Tests der **Tool-Matrix je Agent** (die Lücke, durch die der Befund entstand). **R-DL-16 ✅ done (2026-09-15, `934ea7c`):** `nextIds` ist zustandslos — der Neustart ist unkritisch (Docs = Registry), aber es **reserviert nichts**: gezogen ≠ vergeben, erst das gespeicherte Doc belegt eine Nummer (UC-DL-56/57, Ablauf in `po.md`); der Rückgabewert nennt jetzt Lesequelle + Doc-Dateizahl aus der Discovery. **R-DL-17 ✅ done (2026-09-16, `ab71c53`):** ein `idPrefix` spannt ein **Feature** auf, nicht eine Datei — mehrere Docs dürfen es teilen (das ist die Aussage „gehört zusammen"), `PRAEFIX_DOPPELT` entfällt ersatzlos; Eindeutigkeit sichert `DOPPELT_DEFINIERT`. Löst den Konflikt mit der Doc-Split-Regel aus `po.md` und macht `nextIds` erst sinnvoll (UC-DL-58/59, `UC-DL-17` verbrannt). Offen: Q2 (`suggestIds` separat). **✅ done (2026-09-24, Bug-Report [nextIds-user-bug-report.md](../nextIds-user-bug-report.md), Surefire 949/0/0/0):** R-DL-23 `prefix` mit Bindestrich innen (`[A-Z]+(?:-[A-Z0-9]+)*`, Split am letzten Bindestrich, auch im DocParser/Lint-Pfad) · R-DL-24 Rohvorkommen zählen als belegt, gefundene Form wird fortgeschrieben (flach `OP-79` → `OP-80`), Fundstelle im Vorschlag, „free" nur bei echtem Nichts; Flat-Tie-Break mutations-gepinnt (`2235dba`, Da-Dok C1) · R-DL-25 skipped-Docs namentlich im `nextIds`-Output.
* [Docs als HTML veröffentlichen (Blume)](blume-publishing.md) - **🚧 Idee (2026-09-15, nicht geplant):** `docs/` als statische HTML-Site via [Blume](https://useblume.dev/docs). Vier Kollisionen mit dem Bestand (kein H1 im Content, striktes Frontmatter braucht `frontmatter.extend` für `idPrefix`, Mermaid/Callouts nur in `.mdx`, Tags nur als `search.tags`-Facette). Vorfrage Q-B4: `docs/` ist bewusst intern — veröffentlichen wir es überhaupt?

* [Compact](compact.md) - **Konsolidiert** (2026-09-24, eine Seite für alles Compact-core): **R-CC-1…6 ✅** (Zähler = `inputTokenCount()` statt Kosten, Reevaluate nur bei COMPACTED, `CompactResult` statt stiller Lüge, Hint-Dedup, `~N (estimate)` — ADR-0055) · **R-CIB-1…6 ❌ specified (Redesign 2026-09-24):** Input-Budget = `autoCompactAfter` (Toleranz nur für den Hint) — unter Budget kein Cap; über Budget Stufen: Think-Cap 9000 **front** (`$`-Anker) + nur die letzte echte User-Message (Hint/Marker via Konstanten ausgeschlossen), dann Tool-Results+Args+Think auf 6000 (warn), dann dynamisches Per-Message-Cap `restTokens×7/2/n` (error, garantiert). Schätzung EINMAL (`ChatMessageUtil.estimateTokens` ×2/7). Entry-Debug-Log + Result-Zeile in Log UND ans Agenten; `CompactResult` → Record; Disclosure einmal am Input-Ende. Eigene core-Komponente `compact` ([ADR-0056](adr/0056-compact-component-and-render-modes.md)). **R-CC-7 🚧** (Retry/Fehlerklassen, mit ApiRetry-Tabelle) · **R-CC-8/9 ❌ (Paul 2026-09-24):** Hint/Auto-Gate nur ab > 3 Messages (eine Konstante, überall referenziert), Compact-Reinsert triggert keine Rekursion; „echter User-Text" = letzter TextContent (code-verifiziert, kein Raten).

* [Context Message Konzept](context-message-concept.md) - **✅ done.** ContextItem, Agent-besitzter Compact-Flow, Tool-Delegation, Mixed Restore, Standing Orders Migration. + **✅ done 2026-08-16.** File-Context in der History (Dedup nach vollem Pfad, fehlende Datei → skip, AGENTS.md raus aus dem System-Prompt) — [ADR-0029](adr/0029-file-context-in-history.md), Bugfix-ADR [0030](adr/0030-statictext-helper-frozen-chatmessageutil.md). **✅ 2026-09-11:** Compact-Result genau einmal — Marker `(nothing preserved)` statt Resume-Duplikat + Button-Re-Render autoritativ (Clear bei Erfolg) (`1f2d0b0`/`ce3483d`). **R-ST4 ✅ (`a89cdc6`):** System-Message-Rebuild nach In-Loop-Compact — executeLoop baut staticMessages frisch (AiAgent non-default Interface-Methode), Rest-Turn nicht mehr mit Stale-Prompt/Plan-Snapshot.
* [Tool Descriptions Inventory](tool-descriptions-inventory.md) - **✅ done.** 21/55 `@Tool`-Descriptions optimiert (Konsistenz, Token-Effizienz, LLM-Tool-Auswahl).
* [Chat Markdown Links](chat-markdown-links.md) - **✅ done.** Klickbare Dateiverweise im Chat öffnen im Eclipse Editor (Workspace-Pfade, relative Doc-Links, Fallback-Suche, externe URLs im Browser).
* [Tool-Confirmation](tool-confirmation.md) - **⏳ geparkt (2026-09-19, Paul):** kategorisiertes Bestätigungsmodell mit Scope-Cache Once/Session/Global — Bestätigungen selbst nicht gewünscht (immer aus); Bedarf erst mit Debugger/Background-Shell.
* [Terminal-Session-Tool](terminal-session-tool.md) - **⏳ geparkt (2026-09-19, Paul):** persistente Shell-Session (env/cwd erhalten) statt 60s-One-shot, optional Background + Output-per-ID — Voraussetzung Async-Tool-Infrastruktur (Revisit mit async-agent-tools-proposal).
* [Java-Debugger-Tool](java-debugger-tool.md) - **✅ done (2026-09-21, Paul specified 2026-09-19):** Debugger-Tool für Da Mek, lesend + ändernd (alle Actions), **keine Confirmations** — Debug-Session ist User-Property; sync, keine Session = ehrlicher Fehler. Basis JDT-statt-DAP ([ADR-0050](adr/0050-debugger-jdt-not-dap-user-session.md)), automatisiert nur UC-JD-1 + Stub-Tests, UC-JD-2…6 manuell ([ADR-0051](adr/0051-debugger-no-live-session-tests.md)), API-Drift 2026-09 ([ADR-0049](adr/0049-jdt-debug-2026-09-api-drift.md)). E2E-Smoke 2026-09-21: Bugs F1 (CU-Load) + F7 (Selbst-Exit) gefixt. **2026-09-22 (✅ R-JD-11/12, `b04f0e3`/`26538b0`/`66c1050`):** `list_breakpoints` (15. Action, auch ohne Session — Breakpoint-Landkarte gegen Phantom-BPs, gewähltes Projekt, Exception-BPs workspace-wide disclosed) + `hitCount`-Anzeige-Clamp (-1→0). E2E-Nachbefunde (2026-09-22): „4. Hit" = JDI-Semantik (Suspend-Position = stiller Hit #1, e2e-Doc korrigiert), Exception-BP/HitCount-B-Pfade = Re-Run mit Clean-State offen. **2026-09-23 (❌):** Rename auf `debugJava*` camelCase ([ADR-0054](adr/0054-tool-naming-camelcase-family-prefix.md)) + R-JD-13 Breakpoints ohne Session (Marker-Op: set/remove/exception-BP setzen Marker auch ohne Session, ehrliche Response).
* [Compact-Lock](compact-lock.md) - **❌ specified (2026-09-22, Paul):** User-getriggerter Compact belegt das `working`-Flag (CAS-bedingt) — Agent „working" im Roster, Eingaben gequeued statt parallel (Memory-Race aus chat-job-lifecycle Backlog strukturell behoben), Follow-up-Turn nach dem Compact verarbeitet die Queue FIFO (auch bei Compact-Fehler). Kein CompletableFuture, keine neuer UI-State.
* [Project-Problems-Tool](project-problems-tool.md) - **✅ done (2026-09-20, Paul specified 2026-09-19):** Datei- + Severity-Filter auf `eclipseReadProjectProblems` („validate after edit", token-schlank statt projektweiter Liste) — projektweite Lesung bleibt unverändert. Review-Consensus ergänzt: ungültiger Severity = ehrlicher Fehler, case-insensitive, whitespace-only = unset (R-PP-5…7, Fix `a721313`/`e98e646`).
* [Tool-Output-Disclosure](tool-output-disclosure.md) - **✅ done (2026-09-20, Paul specified 2026-09-19):** Caps ehrlich machen — `eclipseSearchFiles` (Cap **500**, Disclosure im Return), `diskSearchFiles` (Limit-Disclosure), `webFetchAsMarkdown` (paginiert + Fehlerpfad = Snippet statt Body). Ein S-Inkrement.
* [Tool-Time-Disclosure](tool-time-disclosure.md) - **✅ done (2026-09-22, Paul):** Dauer + Uhrzeit am Output-Ende für langlaufende Tools (`CallStats`-Helper im core, [ADR-0053](adr/0053-call-stats-shared-helper.md)) — Shell, `eclipseRunJavaTests` (Rename 2026-09-24, `909ce05`: vormals `eclipseRunTests` — der Java-only-Guard (`JavaCore.create`+`exists()`, nicht-Java-Projekt wirft) macht den Scope explizit), `eclipseBuildProject`; PoDelegateTool migriert (Pin), Timeout-Meldungen tragen die **gemessene** Dauer. Core 916/0, Plugin 264/0. WebFetch-Zeit/Memory-Datum/Debugger-continue geparkt (open-points.md).
* [Web-Tools](web-tools.md) - **✅ done (2026-09-20, Paul specified 2026-09-19):** `webFetchAsMarkdown` paginiert (Mini-Cache 5 URLs, 500-Zeilen-Fenster mit Disclosure) + neues **`webGet(url, path)`** — Download auf Disk, Return = HTTP-Status + Größe + Pfad, Inhalt nie im Kontext; QualifiedPath + WriteValidator-Guards, isEditTool. Architektur: [web-tools-architektur.md](web-tools-architektur.md).

## Notes

* [Verbesserungen aus opencode](verbesserungen.md) - **📝 Studie (2026-08-23).** Top 5 Übernahme-Kandidaten aus opencode: Ox-Alpha-Free-Provider, Modell-Katalog, Layered Activation/Onboarding, Secure Credentials + deklarative Auth, Permissions als Daten.
* [Open Points](open-points.md) - zurückgestellte/offene Punkte mit Status ❓ offen · ⏳ selbst entschieden · 🔒 geklärt.
* [Resolved Points](resolved-points.md) - geklärte Punkte ohne eigenes Feature-Doc (Punkt · Entscheidung · Begründung · Datum). **Tool-Evolution-Run 2026-09-19:** CR-Verdicts nach PO-Run (abgelehnt/geparkt/angenommen + Decompiler-Begründung).
* [Open to Discuss](open-to-discuss.md) - ambiguous items not clear as bugs or features yet; reviewed end-of-cycle.
* [Issue #142 — ASM-Konflikt](issue-142-asm-conflict.md) - **🚧 Analyse (2026-09-19):** unser p2-Repo liefert asm 9.10.1 mit (includeAllDependencies, 2026-09-Target) — meine alte „nicht unser Bug"-Analyse unvollständig; Fix-Kandidaten + Follow-ups offen.
* [Open Ends / Memory](memory.md) - **Session-Zwischenstand only** (Ergebnis, offene User-Handlungen, Startpunkte der nächsten Session); nach jedem Plan-Zyklus aufräumen. Dauerhafte Lernings gehören nach `AGENTS.md` / `AGENTS-DEV.md` / `skills/` bzw. in die memory*-Tools, projektspezifische Fakten in die Feature-Docs und ADRs.
* [ADRs](adr/index.md) - technical decision records (the agent's long-term memory).

## Offene User-Entscheidungen (2026-09-06)

* **Branch `release-2026-09-06` (3 Commits, von main, nicht gemerged):** Dropdown-Klassen
  gelöscht (`a1d8d35`) · UTF-8-Write **widerlegt** + Regression-Guard-Test (`27c09ad`) ·
  PDE-Skip-Count im `EclipseRunTestTool`-Report (`11f34f6`). Plugin 186/0, Core 651/651.
  Merge → main = User-Entscheidung.
* **❓ Glossar eager** ((b) Turn-Context-Item für Jon+Thinka+Mek) und **❓ buildWithDev-Compact**
  (Compact statt Reset, ~50 %, nur neuer Plan) — Empfehlungen in [open-points.md](open-points.md).
* **❓ ApiRetry-Verdacht** (Null-Byte-IOException als Cancel klassifiziert?) — in
  [open-points.md](open-points.md), Investigation im Bug-Fix-Zyklus (Triage #5–#16 + Plugin-Hunt).
