# Plan: Deprecated APIs entfernen aus AbstractAgent + AiAgent

**Ziel:** Alle deprecated APIs aus `AbstractAgent.java` und `AiAgent.java` entfernen, Tests auf neue ContextItem-API umstellen, Build grün.

---

## Context

4 Deprecated APIs erzeugen 40+ Compiler-Warnings und veralten den Code:
- `setStaticContext(Collection<ChatMessage>)` — nur PeonAiService (plugin) + 1 Test
- `setUserContextInformations(Collection<String>)` — nur Tests (Shim zu turnContextSupplier)
- `getUserContextInformations()` — nur Tests (40+ Warnings)
- `getCompressCallback()` — 0 Referenzen

**SimpleMessage.Type (PROBLEM/DIFF):** Nicht im Scope. PROBLEM wird in `AiMonitor.onProblem()` (core) verwendet → muss in core bleiben. DIFF ist nur plugin → könnte auswandern, aber Aufwand > Nutzen. Offen als TO-DO.

---

## Increment 1: getCompressCallback() löschen ✅ DONE

**Status:** Methode gelöscht, 0 Referenzen, Build grün.

---

## Increment 2: Tests auf setTurnContextSupplier umstellen ✅ DONE

**Status:** 2 shim-Tests gelöscht, 6 Tests umgestellt auf setTurnContextSupplier + getRenderedTurnContext(). JonDelegateToolTest: 13 Assertions + 2 Token-Counts angepasst. 379 Tests grün.

---

## Increment 3: setStaticContext aus PeonAiService migrieren ✅ DONE

**Status:** PeonAiService.setStaticContext intern auf setPersistentContext umgestellt (ChatMessage → SimpleContextItem via ChatMessageUtil.toString()). Build grün.

---

## Increment 4: Deprecated APIs aus AbstractAgent + AiAgent entfernen ✅ DONE

**Status:** AiAgent: setStaticContext, getStaticContext, setUserContextInformations, getUserContextInformations entfernt + getRenderedTurnContext() Default. AbstractAgent: Alle 4 deprecated Methoden + staticContext Feld + LinkedHashSet/SimpleContextItem Imports entfernt. CompactSessionToolTest: 3 @Override Annotations entfernt. 379 Tests grün.

---

## BDD Akzeptanzkriterien

| Szenario | GIVEN | WHEN | THEN | Test |
|----------|-------|------|------|------|
| S1: getCompressCallback weg | Methode existiert | Increment 1 | 0 Referenzen, Build grün | `mvn clean verify -pl llmpeon-core` |
| S2: Tests ohne Deprecated APIs | 40+ Deprecation-Warnings | Increment 2 | 0 Deprecation-Warnings in Tests | `mvn clean verify -pl llmpeon-core` |
| S3: PeonAiService migration | `setStaticContext` verwendet deprecated API | Increment 3 | Intern `setPersistentContext`, Behavior identisch | `PeonAiServiceTest` |
| S4: Deprecated APIs gelöscht | 4 deprecated Methoden in AbstractAgent/AiAgent | Increment 4 | Methoden + Felder weg, Build grün | `mvn clean verify` |
| S5: getRenderedTurnContext funktioniert | Agent mit turnContextSupplier | Increment 4 | `getRenderedTurnContext()` gibt gerenderte Items zurück | `JonDelegateToolTest` (13 Assertions) |

---

## Test-Strategie

- **Increment 1:** Nur Core-Build — keine Tests betroffen
- **Increment 2:** Alle betroffenen Tests müssen grün sein:
  - `AbstractAgentTest` — 2 Tests gelöscht, 2 umgestellt
  - `AiDeveloperAgentTest` — 2 Tests umgestellt + Assertions angepasst
  - `JonDelegateToolTest` — 13 Assertions auf `getRenderedTurnContext()`
- **Increment 3:** Plugin-Tests:
  - `PeonAiServiceTest` — setStaticContext wrapper-Test
- **Increment 4:** Vollbuild — 0 Errors, 0 Deprecation-Warnings in production code

**Test-Timeout:** Keine expliziten Waits nötig — alle Tests sind synchron mit mockten LLMs.

---

## Regeln & Constraints

- **Kein Verhalten ändern** — nur API-Flächen aufräumen
- **PeonAiService.setStaticContext bleibt als Wrapper** — Plugin-Clients nutzen sie weiterhin
- **getRenderedTurnContext als Interface-Default** — ermöglicht Test-Assertionen ohne Reflection
- **staticContext Feld entfernen** — nur noch persistentContext (ContextItem-basiert)
- **SimpleMessage.Type nicht anfassen** — PROBLEM in core (AiMonitor), Aufwand > Nutzen

---

## Offene Fragen

**SimpleMessage.Type.DIFF in core?**
- PROBLEM: Bleibt in core (`AiMonitor.onProblem()` default-Implementation)
- DIFF: Nur in plugin (ChatMarkdownWidget.showDiff) → könnte in plugin-eigenen Typ wandern
- **Entscheidung:** Nicht im Scope. TO-DO für später — kein Build-Risiko, nur Sauberkeit.
